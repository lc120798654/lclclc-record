使用openspec+opencode开发

### 功能

支持证书管理，上传证书并把证书部署到数据面，替换数据面默认的证书，支持开启双向认证，并使用证书管理上传的CA进行校验。同时还支持国密证书和国际证书。

### 背景

X网或X网迁移，证书变了，但是客户端不想改，所以以前是手动，但是每次升级版本之后又要手动替换一次，为了不这么凑活，提了个需求，支持页面上上传证书进行替换，另有一个安全需求需要支持双向认证

### 管理面

新增增删改查部署取消部署这几个接口。

增：校验：配额校验，请求体字段校验，名字唯一性的校验，证书有效期、格式等等校验。还校验通过CertificateFactory解析成X509证书，私钥支持pkcs8，通过KeyFactory读取，还支持密码。有两种类型，一种是服务端证书，证书内容和key必填，还有一种是客户端ca证书，只需填CA内容就行。对于服务端证书，还校验公私钥是否配对。（通过先加密，后解密的方式）。通过CertificateFactory解析之后，存到数据库和redis中，数据库中的证书内容：san，commonname，密码算法名称，过期时间，key（使用scc加密），证书密码（使用scc加密），redis中的是一个hash，key是证书id，field就是各种字段，比如content，value是具体的值。

改：校验基本一样，修改redis和数据库中的内容。部署中和取消部署中，已部署的证书不能改。

删：删redis和数据库中的内容，部署中和取消部署中，已部署的证书不能删。

部署：对于服务端证书，是一个string，（同时只有一个能被部署），对于客户端证书，是一个set，里面都是客户端CA的id。部署就是往里面加。

取消部署：取消部署是往部署的里面减。

查：分为分页查询和精确查询详情，查询详情就是根据证书id。查会判断证书的部署状态。数据面已部署（多个节点），每个证书会有一个state带证书id这个key，是hash，field是brokerId，value是1或0,1代表已部署，0代表未部署。查询时根据部署的key判断方向，若部署的key没有，证明是取消部署中或未部署，根据node表查所有节点，对应每个节点的broker若存在1，则为取消部署中，否则为未部署。若部署的key有，证明是部署中或者已部署，若brokerId的数量等于node表数量且匹配（每个node都部署了），则为已部署。否则为部署中。

数据面：启动的时候，读取已部署的证书信息，用各种factory，生成sslcontext，sslcontext add到pipeline中，pipeline生成serverbootstrap，serverbootstrap绑定端口启动。sslcontext用一个atomicReference存在内存中，每次建立连接的时候，都会调用initchannel，从atomicReference中读取现在的context，根据各种开关例如是否开启双向校验，判断如何处理。（netty自带的校验模式，None、optional、required）

另外还有一个逻辑：开启国密和关闭国密，根据redis与内存中的一个状态对比，判断开启还是关闭，开启走一波serverbootstrap那一套流程，关闭从channel列表把channel都关闭，国密用单独起一个端口19443。这个端口注册是在monitor.py中，monitor.py自己判断端口是否存在，存在则向etcd存端口信息，haproxy读取。

有个定时任务（1分钟），一旦证书修改了，就改这个atomicReference，如何判断修改：（redis中的证书内容与内存中的不一致）

如何判断证书是国密还是国际（证书协议withSM2结尾）

CertificateFactory ：解析证书的，国际用jdk自带的，国密用Boucycastle带的SM2密码算法。

KeyFactory：解析明文Key的，

以上两个管理面数据面都用

cipher   cipher.dofinal，通过公钥加密，私钥解密，判断公私钥是否匹配。（只有管理面用）

KeyStore：公私钥放入KeyStore

KeyManageFactory：生成sslcontext的，客户端校验服务端，把包含公私钥的KeyStore放入KeyManageFactory，生成sslcontext（国际用jdk自带的，国密用注册的BGMProvider（华为自己openeuler的一个库，实现了GMTLS这个协议）

TrustManageFactory，生成sslcontext，用于校验客户端的

证书指纹：就是创建设备的时候存入数据库和redis，若开启了开关，连接的时候从channel中读取这次客户端使用证书，从redis中读取证书指纹，看是否匹配。这是为了虽然CA校验过了，但是这个证书不是证书指纹的那个证书，是同一个CA生成的其它证书，或者上传的多个CA中的另一个CA的。

