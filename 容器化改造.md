### 背景

自规划需求

原来数据面的实例都是虚机部署的，扩容比较慢，原来不支持缩容，而且扩容经常失败

改成下一代romaconnect：新的页面、新的服务业务流、增加RCU的概念，1个RCU对应一个容器

原来数十分钟，现在几分钟内（如果不涉及新节点的话，更快）

创建实例流程，原来 使用taskmgr框架，定义一个xml，xml中包含若干task，这些task都对应一个类的实现，这些实现都继承一个task的抽象类
task由job调度执行，支持自动回滚
每步只负责创建自己那份资源，失败时逆序清理，避免资源泄露
context对象是task之间的数据载体，每步往里面写结果，后续步骤继续使用

管理面通过脚本登录数据面，公钥在数据面，私钥在管理面，一开始是默认的，制作操作系统镜像时预置默认公钥，并禁止密码登录，后来起任务定时轮换

旧实例步骤
检查配额，资源租户，ecs，evs，vpc资源租户的
创建vpc，子网，安全组，安全组规则 入是8419，22，出是所有
创建后保存信息到数据库中 vpc id，子网id，安全组规则信息等
查询dcs规格，创建dcs
创建ecs 存信息到node表中
添加磁盘，添加网卡，启动检查dcs任务，启动数据面脚本，从obs上下载安装包
通过ssh执行安装部署命令
失败从数据面取回日志到管理面
绑定公网ip
更新实例状态

### 相关功能

#### 流水线的构造

专门打镜像，又分为公有云镜像和hcs/hcso镜像，之后又搞了一个site，都是容器化，site把管理面也给容器化了

流水线执行一个yaml，yaml可以配在仓库，也可以配在流水线一端，yaml定义生成执行什么脚本，执行机，产物格式，公有云就是镜像、hcs就是一个包，指定image和package

同时还要陪一个私有仓库outrage，还要配账户，上传到swr。

#### 镜像的构建

##### 基础镜像

为什么要搞一个基础镜像：为了加快构建，避免每次构建都重新安装jre、pyhon的各种库，安装rpm和pip的包，有时候因为远端仓库源的问题还可能失败，还可以复用基础镜像。（管里面和数据面都用一个site （存疑））
镜像复用，构建速度，稳定性，可维护性

流水线配一个生成产物使用的脚本，脚本主要就是把代码仓的东西挪到docker构建目录，使用sed修改dockerfile中的from，执行docker build -f

各种安装包有两种途径，一个是yum源，一个是自己传到北冥仓库rpm包（yum源没有）

dockerfile
配置yum源
通过yum源下载python安装包
- 创建用户
- 安装jre
- rpm安装python
- 配置pip
- 安装系统库（logrotate）yum install -y
- 安装python库（supervisor，jinjia2）
- 安装证书（放到一个系统指定目录，然后执行一个命令使其生效，用于curl和wget访问华为自己内部的一些网站）

使用run 反斜杠 &&
cmd是 bash

##### 数据面镜像
hcs除执行docker build外还执行docker save
dockerfile

add .

- 安装scc加解密套件，从流水线参数变为ks文件，从北冥仓库下载seccomponent的rpm安装包
- 执行build脚本打包，与虚机的build脚本一样
- 然后再打成一个包，解压到对应的目录
- 准备路径，改各种路径的权限
安装openssl
使用mvn install构建出tar包
- expose（只起到提示作用）
openssl签名，通过华为的xbuild打包工具打包成rpm包


启动命令是执行supervisor命令使用对应的supervisor文件启动

supervisor配置了eventlistener和启动命令program，管理一个进程，进程挂了给它拉起来

为什么使用supervisor：是跟别的服务学的

supervisor作为1号进程

autostart设置为false，这样启动supervisor启动的时候不会自动执行启动脚本，先启动eventlistener里面的monitor脚本进行一些初始化工作

eventlistener里面执行的是monitor脚本，有个trap命令kill掉supervisor，就是说monitor脚本挂了之后，会kill掉supervisor，避免没有monitor.py管理的僵尸的进程，同时认为monitor.py比较重要

monitor脚本执行monitor.py

初始化执行 clean old large logs，一个bug
起子进程subprocess popen执行命令，打印日志，使用tempfile代替subprocess.pipe，避免超出管道大小导致阻塞

使用unix///var/log supervisor supervisor. sock，套接字通过xmlrpc库创建传输对象，并配置代理，用于rpc访问

把容器的环境变量的各种参数配置到配置文件中

site专有，配置kafka客户端创建固定topic

runforever执行
supervisor事件监听器协议基于标准输入输出，childutils.listener.wait从stdin读取，ok写入stdout
如果shell脚本内部再执行python脚本，python进程会继承shell的stdin和out，所以wait和ok都行
监听 process state fatal 进程致命错误 直接退出
process group added 进程组添加 启动前
supervisor 启动的是非登录shell 进程运行时环境变量不全，需要source etc profile
修复因信号量scc启动失败问题 清理root用户的system v信号量集 ipcs -s grep root awk print $2 xargs  -r ipcrm -s
因为autostart是false ，因此pre start通过rpc调用starprocessgroup显式启动业务进程
启动成功后发送progress state running
修改一些文件和日志的权限

tick 60，执行logrotate命令，保留50份,50m，上报统计信息ces
site专有，初始化etcd客户端

创建下一代实例 创建前已创建好cce集群，runtime文件工程已调用接口上传至server数据库，包括两个json

pre config 请求体，包含实例id，project id，实例名称，vpcid 子网id，rcu数量等
新建实例，填充实例表，生成key secret秘钥，用于调实例的接口，apig hmac签名验证，管理面用key secret对请求做签名，数据面用同样的key secret验签
返回管理租户相关 appkey appsecret等，实例状态设置为creating

postdeploy。连接地址 dcsid等
刷新实例表，状态设置为创建成功，复用node表，只有一个节点

同步实例etcd数据

模板 deployment matchlables metadata lanles
volume
参数 appkey secret redis ces hostilememoryrequest xmx为7分之16
downloadapi获取hostip
request cpu 3u memory 4g
limit cpu 3.5u memory 6g
一个节点是8u16g
qos机制，limit达到会发生什么
volumemounts 日志采集规则
三个探针，十秒检查，3次失败，超时1s
启动探针15秒延迟12次
拉取策略
重启策略
优雅退出30s
不自动挂载serviceaccounttoken
securitycontext 默认
亲和和反亲和
容忍污点，节点会设not ready和节点不可达，未就绪容忍5分钟
滚动升级策略，1和0
节点标签，实例id和负载类型
roma最佳实践，停车场

容器化后一些适配工作 原来ip是固定的，现在到处飘，不固定，原来有一个brokerid，是由ip拼起来的，改了之后使用uuid
redis中，poolset，amount，privateip
数据面启动的时候，从里面拿个未使用的，使用分布式锁，为什么拿未使用的，主要是为了和原来保持一致，节点重启后还拿原来的，最小化改动，避免重启几次换几个brokerid
通过state和privateip两个状态判断，同时如果是自己的直接获取
全都被用则自己创建一个新的
获取到brokerid后，执行一些在线设备数量的清理，起brokerid对应ip的定时任务，30s检查，维持90
还有30s定时任务，Amout数量小于brokerpool后清理

可能会发生的情况，同一个brokerid被两个节点拥有，redis上下电，gc超过90s，导致新节点误认为某个brokerid未被使用
解决措施，当发现时候，一个就得自杀，写redis key value owner podname starttine，维护状态的30s定时任务判断拥有者是否不是自己了，不是自己自杀，另外为了避免定时任务的30s真空期，重要写入，影响较大的订阅关系，设备上下线数量等，也判断下，同时逻辑避免10s内重复判断
该方案也比较麻烦，而且有锁还要抢锁，启动时间也慢

继续优化，不用uuid，还是用ip，因为pod重建很可能仍然使用一个ip
主要原因，经过分析，残留数据都会自愈，设备重连时，increaseandgetclientsession。写入新brokerid的session，重连过程中旧session被清理，subscriptionbrokers被更新
若没有brokerpool，会有残留东西，后经过研判，基本可以自愈，包括订阅关系等，一般客户端会自动重连加建立订阅，只需对统计类key的残留进行清理
mqtt broker，属于半有状态，除了设备连接channel外，其它在redis中，订阅，配置，broker状态等
brokerid的用途 设备在线数量，订阅关系，强制下线

分布式锁 先通过setnx获取锁，value是获取锁的时间
获取到之后设置value expired
没获取上，重复获取，直到时间到，时间到后，先get一下，如果get为空，证明最后一次时候到期了，然后再getset一下，若返回非null，证明这个短时间被别人抢了，退出，获取锁失败，如果getset返回null，获取锁成功，如果get不为空，且时间距现在不为锁时间的两倍，获取锁失败。若超过两倍，getset一下强制抢锁，若返回的不是之前get的值，证明这个抢锁🈶被别人抢了，否则成功

避免删错，例如一个获取锁后，被gc卡主，锁超时被别的获取，又卡好了，之后执行完删除锁，删成别人的。因此value除了时间还要设置一下属于谁。删除的时候原子的判断下是否是自己的并删除。

redis主从切换可能丢锁，锁写在主上了，主给从同步是异步的，读的时候从从节点读。即使从节点没来及同步，但是由于是setnx，所以只会在主节点进行，因此不会出现虽然主节点获取了锁，但是从节点没来及同步，读到从节点没锁的情况，因为setnx只会在主节点进行。
但是主从切换可能存在问题。因为主写的锁，可能还没同步到从里，从变成了主，导致下一个客户端从新的主读取时还是没锁，导致双写
解决 在数据库中设置单增的fencing token，同时在数据的资源也有一列fencing token，获取自己的ft是原子增的，之后再写入资源的时候，获取资源的ft和对资源的ft的写入也需要是原子的，这样修改某个项目时，可以一遍设置ft，一边查询，例如update orders set status cancelled fencing token等于100where order id等于122and fencing token小于100

statefulset相比deployment的代价 慢一个一个顺序启动，滚动更新慢，调度也麻烦，还需要维护一个headless service，否则无法创建
pvc pod重建要重新挂载原来的卷
postdelete要调用flush db，secret标志，内存中标志，禁用写操作

broker 节点身份与状态
设备在线管理
网关子设备
订阅关系
统计指标

强制下线 管理面调数据面http接口，任一节点收到请求，将要下线的clientid放入队列中，定时任务消费并下线 每次消费50个

Shardingsphere
datasourceprovider spi注册
仅暴露逻辑数据源
继承standardShardingalgorithm的dosharding方法，只实现精确匹配，范围匹配还是全量表
一个segment对象
好处，可多次扩表，不需要迁移
性能要求 接口响应时间小于30s
迁移的问题，不好回滚，高风险，长耗时，需停机
最大主键和记录数都加10000冗余
要求响应时间小于30s
分表好处 索引效率高，并行写入，锁竞争低，单表运维方便，方便继续扩展
问题 深分页查询慢

连接池数量50，查询并发10

下界 当前最大主键加所有记录数除以旧表的数量乘新表的数量
每次加一个过渡区间和平衡区间，最后一个是平衡区间

背景，IoT低删除场景，否则不停增删主键会超过过渡区间
方案取舍




遇到的问题
mysql没有全局序列作为主键
使用Shardingsphere的customsharding，根据数据库不同执行不同的mapper，dm和gauss执行nextval，mysql往device id表插一条数据，只有主键和更新时间，

一个普通数据源，一个sharding数据源，绑定的mapper不一样，

异步任务 去掉mysql的device表的device oid的自增属性，避免出现问题

设备在线状态更新并行 表id为key，要更新的设备id为value，因为连接池压力大，容易锁竞争

abstractroutingdatasource 建立连接热替换map选择实际的数据源

mapper sessionfactory session

dm扩展，不原生支持，使用了三个spi扩展，解析，优化器，使用sql方言

